{
  "name": "example",
  "version": "0.0.1",
  "description": "example project for testing frontend-maven-plugin",
  "dependencies": {
    "jquery": "3.6.0"
  },
  "scripts": {
    "test": "web-test-runner src/**/*.test.js --node-resolve",
    "build": "vite build",
    "test2": "pnpm build && pnpm test"
  },
  "devDependencies": {
    "@esm-bundle/chai": "^4.3.4-fix.0",
    "@web/test-runner": "^0.16.1",
    "vite": "^4.3.9"
  }
}
